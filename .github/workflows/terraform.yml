name: Terraform AWS Multi-Account

on:
  pull_request:
    paths:
      - "aws/terraform/environments/**"
      - "aws/terraform/modules/**"
      - ".github/workflows/terraform.yml"
  push:
    branches:
      - main
    paths:
      - "aws/terraform/environments/**"
      - "aws/terraform/modules/**"
      - ".github/workflows/terraform.yml"

permissions:
  contents: read
  id-token: write
  pull-requests: write

jobs:
  terraform-plan:
    if: github.event_name == 'pull_request'
    name: Terraform Plan (${{ matrix.environment }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: dev
            path: aws/terraform/environments/dev
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_DEV }}
          - environment: stage
            path: aws/terraform/environments/stage
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_STAGE }}
          - environment: prod
            path: aws/terraform/environments/prod
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_PROD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Configure AWS credentials (management)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_MANAGEMENT_GHA_ROLE_ARN }}
          aws-region: ap-northeast-2
          role-session-name: gha-tf-plan-${{ matrix.environment }}

      - name: Terraform Init
        id: init
        working-directory: ${{ matrix.path }}
        run: terraform init -input=false
        env:
          TF_VAR_aws_account_id: ${{ matrix.aws_account_id }}

      - name: Terraform Plan
        id: plan
        working-directory: ${{ matrix.path }}
        run: |
          set +e
          terraform plan -input=false -no-color -out=tfplan > plan.txt 2>&1
          PLAN_EXIT_CODE=$?
          cat plan.txt || true
          echo "plan_exit_code=${PLAN_EXIT_CODE}" >> "$GITHUB_OUTPUT"
          exit 0
        env:
          TF_VAR_aws_account_id: ${{ matrix.aws_account_id }}

      - name: Comment Plan to PR
        if: always()
        uses: actions/github-script@v7
        env:
          MATRIX_NAME: ${{ matrix.environment }}
          MATRIX_PATH: ${{ matrix.path }}
          PLAN_EXIT_CODE: ${{ steps.plan.outputs.plan_exit_code }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require("fs");
            const marker = `<!-- terraform-plan:${process.env.MATRIX_NAME} -->`;
            let planOutput = "No plan output generated.";
            try {
              planOutput = fs.readFileSync(`${process.env.MATRIX_PATH}/plan.txt`, "utf8");
            } catch (error) {
              planOutput = `Failed to read plan output: ${error.message}`;
            }

            const status = process.env.PLAN_EXIT_CODE === "0" ? "SUCCESS" : "FAILED";
            const body = `${marker}
            ## Terraform Plan: ${process.env.MATRIX_NAME} (${status})
            \`\`\`
            ${planOutput.slice(0, 60000)}
            \`\`\``;

            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100
            });

            const existing = comments.find((comment) => comment.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body
              });
            }

      - name: Fail if Plan failed
        if: steps.plan.outputs.plan_exit_code != '0'
        run: |
          echo "Terraform plan failed for ${{ matrix.path }}"
          exit 1

  terraform-apply:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    name: Terraform Apply (${{ matrix.environment }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: dev
            path: aws/terraform/environments/dev
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_DEV }}
          - environment: stage
            path: aws/terraform/environments/stage
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_STAGE }}
          - environment: prod
            path: aws/terraform/environments/prod
            aws_account_id: ${{ vars.AWS_ACCOUNT_ID_PROD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Configure AWS credentials (management)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_MANAGEMENT_GHA_ROLE_ARN }}
          aws-region: ap-northeast-2
          role-session-name: gha-tf-apply-${{ matrix.environment }}

      - name: Terraform Init
        working-directory: ${{ matrix.path }}
        run: terraform init -input=false
        env:
          TF_VAR_aws_account_id: ${{ matrix.aws_account_id }}

      - name: Terraform Apply
        working-directory: ${{ matrix.path }}
        run: terraform apply -input=false -auto-approve
        env:
          TF_VAR_aws_account_id: ${{ matrix.aws_account_id }}
