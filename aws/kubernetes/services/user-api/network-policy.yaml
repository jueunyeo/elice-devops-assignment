apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-api-default-deny
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: user-api
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-api-allow-from-gateway
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: user-api
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      ports:
        - protocol: TCP
          port: 8080
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-api-allow-egress-db-and-dns
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: user-api
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              elice.io/tier: data
          podSelector:
            matchLabels:
              app.kubernetes.io/name: postgres
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
